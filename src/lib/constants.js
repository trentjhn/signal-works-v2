// Shared constants for the new page primitives. Existing homepage components keep their
// own inline values; this is only to avoid re-hardcoding the same strings across the
// service/about pages added in the multi-page build.
export const CALENDLY = 'https://calendly.com/hello-signalworks'
export const EMAIL = 'hello@signalworks.live'
